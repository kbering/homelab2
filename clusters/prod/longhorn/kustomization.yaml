resources:
  - namespace.yaml

helmCharts:
  - name: longhorn
    repo: https://charts.longhorn.io
    releaseName: longhorn
    namespace: longhorn-system
    version: 1.7.0        # eller din ønskede version
    valuesFile: values.yaml
    includeCRDs: true