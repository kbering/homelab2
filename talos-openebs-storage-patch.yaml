machine:
  sysctls:
    vm.nr_hugepages: "1024"
  disks:
    - device: /dev/sdb
      partitions:
        - mountpoint: /var/mnt/storage
  kubelet:
    extraMounts:
      - destination: /var/mnt/storage
        type: bind
        source: /var/mnt/storage
        options:
          - bind
          - rshared
          - rw
